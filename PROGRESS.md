# PrivDoc 工作进度存档

最近更新：2026-07-20 CST

## 2026-07-20 保存保护、格式校验与历史隐私修复

已完成：

- 修复新建密档按 `Command-S` 只写入内存、却提示“已保存”的数据丢失路径。
- 首次保存会先提交编辑器最新正文，再进入加密文件保存流程。
- 只有加密文件原子写入成功后才显示保存成功；写入失败会保留未落盘状态并提供“重试保存”。
- 离开未保存编辑，以及锁定、新建或打开其他密档前，会要求用户明确选择保存、放弃或取消。
- 文档密码增加二次确认，避免单次输错后无法解锁。
- 新密档首次加密落盘前只允许查看，避免自动锁定清除尚未保存的用户正文。
- `Command-Q`、菜单退出和系统正常终止都会经过同一套未保存确认。
- 同时存在写盘失败版本和编辑草稿时，放弃操作会明确说明两部分内容都会丢失。
- 自动锁定或系统睡眠前，已有文件会先尝试写入最新草稿；无法落盘时会明确提示内容已清除。
- 修复 Markdown 标题后空行导致标题和字段被拆成两个 Smart Entry 的问题。
- URL 内嵌账号密码、敏感 query/fragment 参数、无端口 IP 面板路径现在默认脱敏，并排除在搜索之外。
- 修复历史原文预览在混用 `:`、`：`、`=` 时可能泄露密钥前缀的问题。
- `.privdoc` v1 读取改为固定参数白名单：密码模式只接受 PBKDF2-SHA256 / 310,000 次 / 16-byte salt；系统模式只接受固定 KDF / 0 次 / 空 salt / UUID `keyID`。
- nonce 固定为 12 bytes，ciphertext 至少包含 16-byte GCM tag；未知 `authMode` 和畸形 JSON 统一拒绝，只有缺失 `authMode` 的旧密码文件保留兼容。
- 解锁与另存加密改用异步 CryptoBox；解锁弹窗增加进行中状态、防重复提交、防关闭和关闭后的临时状态清理。
- 历史 Diff 改用有序 Smart Item 记录，覆盖标题、正文、字段、裸密钥、邮箱、SSH 和 URL；同名重复项不再被字典覆盖。
- Diff、历史预览和自动摘要共用安全投影；敏感值跨字段改名仍继承更严格的显示策略，敏感 URL 解析失败时默认整段隐藏。
- 规则页补齐条目标题词的手动添加、查看、删除和持久化；规则变化后会清理失效的条目选择。

验证：

- `swift test`：75 个测试通过（11 Crypto + 14 Diff + 1 Document + 30 Smart Parser + 19 Store）。

## 产品核心

PrivDoc 不是传统密码管理器，也不是普通加密笔记。它的核心形态是：

> 本地加密的、可操作的隐私文档。

用户仍然像写 txt/Markdown 一样保存信息，但查看时，文档里的 `字段名：字段值` 会被拆成可操作字段。密钥默认不展示明文，只能通过双击复制等主动动作释放。

当前最重要的产品原则：

- 查看优先，编辑克制。
- 密钥默认可用，不可见。
- 字段是对象，不是文本。
- 用户教过一次规则，App 全局都应该懂。
- 新保存优先走文档密码，行为透明、可迁移；系统授权只保留旧文件兼容和后续正式签名验收空间。

## 目标用户

第一目标用户是个人开发者，尤其是本机有一份敏感 txt 的人：

- 记录服务器、OnePanel、数据库、API Key、SSH、恢复码等信息。
- 平时主要是查看和复制，很少新增修改。
- 讨厌 txt 没分类、长字符串不好复制、密码管理器太老太重。
- 需要本地、轻量、可控、现代化的体验。

## 已实现 MVP

工程位置：仓库根目录

可运行 App：

- `PrivDoc.app`
- `./run.sh`
- `./build_app.sh`

已实现功能：

- macOS SwiftUI 原生桌面 App。
- 新建/打开本地 `.privdoc`。
- 查看、编辑、历史、规则、设置五个模式。
- 查看态自动拆分 `字段名：字段值`。
- 支持 `：`、`:`、`=` 三种字段分隔符。
- 密钥字段默认显示为“已隐藏，双击复制”。
- 普通字段和密钥字段都可双击复制。
- 剪贴板清理可配置，支持自动清理和手动清除。
- 自动清理只清除 PrivDoc 自己写入的那次剪贴板内容。
- 支持 `{{...}}` 无理由隐藏任意文本片段。
- 正文里的隐藏片段会以行内隐藏 chip 渲染，双击复制原文。
- 搜索标题、字段名、非密钥字段值。
- 编辑态保留 Markdown-ish 原文。
- 编辑保存会生成历史版本。
- 历史页支持 Smart Item 级 diff 和安全原文预览，密钥值始终隐藏，敏感 URL 只显示安全投影。
- 规则页支持全局识别规则。
- 规则包括自定义密钥词、强制普通字段和条目标题词，均可手动管理并持久化。
- 查看态右键字段可把字段名加入密钥规则或普通字段规则。
- 设置页支持剪贴板清理时间和自动锁定时间。
- 新保存 `.privdoc` 使用文档密码，不写入 macOS Keychain。
- 兼容打开旧版文档密码 `.privdoc` 和旧系统授权 `.privdoc`。

## 2026-07-06 Smart View

已实现：

- 查看态从 section-first 升级为 entry-first。
- 原文不改，查看态解析 Entry / Item。
- 支持字段、邮箱、裸密钥、SSH、1Panel 日志、面板 URL。
- 面板 URL 视觉隐藏随机路径 token，复制保留完整值。
- 搜索排除隐藏值。
- 右键可把字段识别为密钥/普通字段，也可添加条目标题规则。

边界：

- 暂不自动合并重复条目。
- 暂不重写原文。
- 已完成基于 Smart Item 的历史 diff；同名重复章节整体重排仍无法稳定识别移动。

## 2026-07-01 精细化修复

本轮针对真实体验截图做了一次全局 polish。

已修复：

- Toast 复制提示从黑底深字改为浅色高对比浮层，避免“背景和文字糊在一起”。
- Toast 位置改到右下角，宽度受控，不再贴在窗口底部中央。
- Toast 增加状态图标，复制/保存/锁定/错误区分更清楚。
- 历史详情页 header 固定，版本标题、时间和“恢复这个版本”按钮不会被内容滚走。
- 历史详情页只有 diff 和原文预览区域滚动。
- 历史版本左侧长标题改为中间截断，避免撑爆列表。
- 历史 diff 的状态和密钥值文案改为中文。
- 历史原文预览默认脱敏密钥，避免在历史页重新暴露旧 secret。
- 保存无变更时提示“没有内容变更”，不再误导用户以为生成了新版本。
- 恢复版本、规则增删、解锁/保存/锁定等 Toast 文案统一中文。
- App 菜单命令改为中文。

## 2026-07-01 剪贴板策略

新增剪贴板清理配置。

已实现：

- 新增“设置”页。
- 剪贴板清理支持 5 秒、15 秒、30 秒、1 分钟、5 分钟、手动清除。
- 默认仍为 15 秒，保持安全默认。
- 选择“手动清除”时会提示风险。
- 顶部栏在剪贴板含 PrivDoc 内容时显示“清除剪贴板”按钮。
- App 锁定时会尝试清除 PrivDoc 写入的剪贴板内容。
- 清理时会检查 pasteboard `changeCount`，只清理 PrivDoc 自己写入的那次内容，避免误删用户之后复制的新内容。
- 旧版 `clearClipboardAfterSeconds` 设置会自动迁移到新的 `ClipboardClearDelay`。

## 2026-07-01 显式隐藏标记

新增轻量显式隐藏语法。

已实现：

- `{{abc123}}`：无理由隐藏，查看态隐藏，复制时复制 `abc123`。
- `{{abc123}}` 单独成行时，会渲染成一个隐藏段落。
- `备注：公开 {{私密}} 公开`：正文里只隐藏双大括号包住的片段，其他内容保持原样。
- `字段：{{私密}}`：字段值隐藏，复制时复制原始内容。
- `@secret`：行尾标记，强制该字段为密钥。
- `@text`：行尾标记，强制该字段为普通文本。
- `#secret` / `#text`：兼容同类行尾标记。
- 双大括号隐藏优先于行尾标记、用户规则、内置规则和自动类型判断。
- 规则页增加“显式标记”说明和示例。

产品判断：

- 不采用 `<*>...</*>`。那只是讨论时的例子，不作为 PrivDoc 语法。
- 移除 `{{secret:...}}` 设计。用户不需要解释为什么要隐藏一段文本。
- 存储层仍是整个 `.privdoc` 加密；双大括号控制的是解锁后的查看态渲染和复制行为。

细节修复：

- 正文里的隐藏片段 chip 调小为行内徽标，不再使用字段级大胶囊样式。
- 自定义 FlowLayout 放置时改为行内垂直居中，避免隐藏 chip 超出中文文本基线。

验证：

- `swift build` 通过。
- `./build_app.sh` 重新打包成功。
- Computer Use 截图验证受本机权限/远程连接状态限制，未拿到可靠截图；本轮主要通过源码审查、编译和用户截图对应问题修复验证。

## 当前安全实现

加密文件格式：

- 文件后缀：`.privdoc`
- 外层 envelope：JSON
- 明文 payload：JSON 后整体加密
- 对称加密：AES-GCM

文档密码模式：

- KDF：PBKDF2-HMAC-SHA256
- 迭代次数：310,000
- 每次保存使用随机 salt 和 nonce

系统授权模式：

- 生成随机 256-bit vault key
- vault key 存入 macOS Keychain
- Keychain 访问控制要求 user presence
- 打开时通过 Touch ID 或 Mac 登录密码授权

重要边界：

- 系统授权模式体验更好，但绑定当前 Mac 的 Keychain。
- 系统授权 `.privdoc` 直接拷到另一台电脑无法解锁。
- 文档密码模式可迁移，但需要用户记住密码。
- 当前已有自动锁定；还没有内存安全清理、Argon2id。

## 关键产品决策

### 1. 不是做 KeePassXC 翻版

方向不是补齐传统密码管理器功能，而是解决“隐私文档使用不爽”：

- txt 没分类。
- 长字符串不好选中复制。
- 修改后怕搞坏。
- 密钥查看时容易暴露。

### 2. 查看与编辑分离

隐私文档大多数时候用于查看和复制，不是编辑。

默认进入查看态，编辑必须明确切换。这样减少误改，也让界面围绕“使用字段”而不是“写文档”设计。

### 3. 密钥默认不显示

密钥不是拿来看的，是拿来用的。

查看态里密钥字段默认只展示用途，不展示内容。双击后静默写入剪贴板，只提示已复制。

### 4. 原文仍是真相

编辑态不是复杂表单，而是 Markdown-ish 原文。系统解析原文生成查看态字段。

这保留了 txt 的自由度，同时让查看态拥有结构化体验。

### 5. 规则是全局配置

最初考虑过把识别规则写入每份 `.privdoc`，后来改成全局配置。

原因：从 UX 看，用户是在训练 App 理解自己的写法，不是在配置某个文件。用户教一次 `授权码` 是密钥，之后所有文档都应该懂。

### 6. 新保存默认用文档密码

曾经尝试把系统授权作为默认模式：

- 默认系统授权。
- 主动选择文档密码。

但开发版可能出现 Keychain 权限错误或钥匙串访问确认弹窗，信任感不好。当前新保存流程回到文档密码；旧系统授权文件仍可打开，方便迁移。

### 7. 自动锁定是自用底线

PrivDoc 处理的是已经解锁的敏感内容，不能只依赖用户手动锁定。

当前已实现：

- 解锁或新建后启动自动锁定计时。
- 鼠标、键盘和滚动等本 App 内活动会刷新计时。
- 设置页可选择 1 分钟、5 分钟、15 分钟、30 分钟。
- Mac 睡眠、锁屏或用户会话离开时立即锁定。

## 主要源码文件

- `Package.swift`：Swift Package 配置。
- `Sources/PrivDoc/PrivDocApp.swift`：App 入口。
- `Sources/PrivDoc/Models.swift`：数据模型。
- `Sources/PrivDoc/DocumentParser.swift`：文档解析和字段类型识别。
- `Sources/PrivDoc/CryptoBox.swift`：加密 envelope、AES-GCM、PBKDF2、系统授权加密接口。
- `Sources/PrivDoc/KeychainVaultKeyStore.swift`：macOS Keychain vault key 保存/读取。
- `Sources/PrivDoc/VaultStore.swift`：App 状态、打开保存、复制、历史、规则管理。
- `Sources/PrivDoc/ContentView.swift`：SwiftUI 界面。
- `Sources/PrivDoc/DiffEngine.swift`：字段级 diff。
- `README.md`：运行说明。
- `run.sh`：开发运行脚本。
- `build_app.sh`：打包 `PrivDoc.app`。

## 2026-07-01 系统授权保存修复

用户反馈系统授权保存时出现 `-34018`，含义是缺少 Keychain entitlement。

已修复：

- 系统授权模式保存前，先主动拉起 macOS 身份验证。
- 授权通过后再创建随机 256-bit vault key。
- vault key 写入 Keychain 后，再写入 `.privdoc` 文件。
- 保存按钮增加“保存中...”状态，防止重复点击。
- Keychain 错误文案改为中文，并在 `-34018` 时提示改用“文档密码”或使用正式签名版本。

后续修正：

- 曾尝试给 adhoc 签名的 SwiftPM app 加 `keychain-access-groups` entitlement，但 macOS 会直接拒绝启动，表现为“应用程序无法打开”。
- 已撤销 `PrivDoc.entitlements`，`build_app.sh` 恢复为普通 adhoc 签名。
- 当前 `PrivDoc.app` 已验证可以启动。
- 开发期不硬加 Keychain group；正式发布时应使用 Xcode 项目和 Developer ID/Team ID 正式签名后再配置 Keychain entitlement。

## 2026-07-01 查看页目录语义修复

用户反馈查看页左侧 `Section` 意义不明，点击“全部”只看到标题，且第一行一级标题不应该被当成一块 Section。

已修复：

- 左侧标题从 `SECTIONS` 改为“目录”。
- 左侧入口从“全部”改为“全文”，语义更像阅读整篇文档。
- `# 一级标题` 现在只作为文档标题，不进入目录。
- `## 二级标题` 才作为目录项。
- 点击“全文”会显示文档标题和所有目录内容，不再只显示第一块。
- 点击某个目录项只显示该目录下的内容。
- 如果文档没有 `##`，正文内容会归入隐式“正文”块；纯标题和空行不会生成无意义目录项。
- 移除旧的 `parseWithDocumentSection` 解析路径，避免 H1/H2 语义混乱。

产品判断：

- 一级标题回答“这份密档叫什么”。
- 二级标题回答“这份密档有哪些可跳转的内容块”。
- 查看页左侧不是技术概念里的 section 列表，而是用户理解里的目录。

## 2026-07-02 App Store / Keychain 体验判断

围绕系统授权保存时出现的 Keychain 弹窗，做了一次产品体验判断。

当前开发版现象：

- 使用 Touch ID 授权后，仍可能出现 `-34018` 或 Keychain 权限错误。
- 也可能出现 macOS 弹窗：`PrivDoc 想要使用你储存在钥匙串中的 "local.privdoc.vault-key" 中的机密信息`。
- 这个弹窗感官很差，用户会理解成“App 在读取系统钥匙串里的机密”，不适合作为默认用户体验。

技术判断：

- Touch ID 只证明“当前操作的人是用户本人”。
- Keychain entitlement 证明“这个 App 是否有资格访问自己名下的钥匙串项”。
- 当前 SwiftPM / adhoc 签名开发包没有完整 Keychain entitlement，所以通过 Touch ID 后仍可能被 macOS 拒绝。
- 不是安装时把权限“植入系统”，而是正式签名后的 App 通过稳定 Team ID、Bundle ID、provisioning profile 和 entitlements 访问自己的 Keychain access group。

正式发布判断：

- 如果后续上架 App Store，并正确配置 Keychain Sharing / keychain access group entitlement，`-34018` 和“当前 App 缺少 Keychain 权限”理论上不应该再出现。
- Touch ID / Mac 登录密码确认仍会存在，这是 `userPresence` 安全策略的一部分。
- 不能只凭理论承诺“绝不会出现 Keychain 字样”。必须用 App Store/TestFlight 或正式签名包，在干净 macOS 用户环境里实测。

上线前验收标准：

- 保存系统授权密档时，只出现 Touch ID 或 Mac 登录密码确认。
- 重新打开系统授权密档时，只出现 Touch ID 或 Mac 登录密码确认。
- 全流程不能出现“想要使用你储存在钥匙串中的机密信息”这类 Keychain ACL 弹窗。
- 全流程不能出现 `-34018` 或缺少 Keychain 权限提示。

产品决策：

- 只有正式签名包通过上述验收，系统授权才适合作为默认保存模式。
- 如果正式签名后仍出现 Keychain ACL 弹窗，则默认保存模式应改为“文档密码”，系统授权放到高级选项，并明确说明密钥会保存到本机 macOS Keychain。

## 已验证

最近一次验证：

```bash
swift test
git diff --check
codesign --verify --deep --strict PrivDoc.app
plutil -lint PrivDoc.app/Contents/Info.plist
```

结果：75 个测试全部通过，差异格式、App 签名和 `Info.plist` 均通过校验。

最近一次打包：

```bash
./build_app.sh
```

结果：生成 `PrivDoc.app` 成功。

UI 验收说明：

- 已通过 Computer Use 操作 Release `PrivDoc.app`，使用独立临时密档，不打开现有用户文件。
- 新密档点击“编辑”或“规则”都会先弹出首次保存；取消后仍在查看页，保存成功后会继续进入目标规则页。
- 条目标题规则空状态不显示“内置规则”，添加和删除均有正确反馈，删除后恢复空状态。
- 错误密码会显示错误并恢复可操作状态；用 Esc 关闭后再次打开，旧密码和旧错误均已清理；正确密码可以解锁。
- “解锁中...”只持续约百毫秒，自动化读树没有截到瞬时帧；按钮文案、禁用条件、防重复提交和进行中禁止关闭已通过源码路径与回归测试核对。

## 待做优先级

最值得下一步做的不是大功能，而是把真实自用体验打磨顺：

1. 锁定体验
   - 自动锁定前可选提示。
   - 锁定后恢复工作流更顺。

2. 解锁方式转换
   - 系统授权 -> 文档密码。
   - 文档密码 -> 系统授权。
   - 避免用户一开始选错后没法改。

3. 正式签名 Keychain 验收
   - 建 Xcode / App Store 打包配置。
   - 配置 Keychain Sharing / keychain access group entitlement。
   - 用正式签名包在干净 macOS 用户环境验证系统授权弹窗。

4. 导入体验
   - 从现有 txt 导入。
   - 首次导入后给解析预览。
   - 提示疑似密钥字段。

5. 密钥字段编辑体验
   - 查看态右键“替换密钥”。
   - 编辑态密钥默认隐藏。
   - 修改 secret 不必先显示旧 secret。

6. 规则增强
   - 支持导入/导出全局规则。
   - 支持字段值模式，比如 `sk-`、`ghp_` 自动识别为密钥。

7. 快捷搜索窗口
   - 全局快捷键呼出。
   - 搜索字段。
   - 回车复制默认字段。

8. 安全增强
   - KDF 从 PBKDF2 升级到 Argon2id。
   - 更明确的剪贴板管理提示。
   - 检测剪贴板管理器风险。
   - 更完整的内存清理。

## 当前风险和注意事项

- 这是 MVP，可以开始自用，但真实数据迁入前建议保留原 txt 离线备份。
- 系统授权模式下，必须保留当前 Mac 的 Keychain；迁移文件前要转换成文档密码模式。
- 当前开发版的系统授权体验不能代表正式版；是否默认使用系统授权，必须等正式签名包通过 Keychain 弹窗验收后再定。
- 自动锁定已经实现，但真实敏感数据使用时仍建议离开电脑前手动锁定。
- 历史版本会保存旧文档内容，删除/修改密钥后历史里仍有旧值，虽然 diff 脱敏且文件整体加密。
- 规则是全局配置，不跟 `.privdoc` 走；换机器后需要重新配置，后续应做导出/导入。
- v1 envelope 的元数据还没有作为 AES-GCM AAD 参与认证；彻底解决需要升级文件格式并保留旧文件迁移路径。
- 同名重复章节缺少持久化 ID，整体重排时不能稳定判断移动关系。
- 历史版本按当前全局规则重新解析；删除旧的自定义密钥规则可能改变历史展示，后续需要给规则或敏感性快照做版本化。
- 手动填写的版本备注按公开元数据原样显示；不要把密钥写进版本备注。自动生成的摘要已使用安全投影。

## 下一次继续时的建议入口

先体验真实数据的一小段，不要一次性全量迁入。

推荐下一轮优先做：

1. 解锁方式转换。
2. 导入 txt + 解析确认。
3. 密钥字段编辑体验。

这三个会直接决定它能不能真正替代原来的敏感 txt。
