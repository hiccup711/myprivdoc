# PrivDoc 工作进度存档

最近更新：2026-07-01 15:25:54 CST

## 产品核心

PrivDoc 不是传统密码管理器，也不是普通加密笔记。它的核心形态是：

> 本地加密的、可操作的隐私文档。

用户仍然像写 txt/Markdown 一样保存信息，但查看时，文档里的 `字段名：字段值` 会被拆成可操作字段。密钥默认不展示明文，只能通过双击复制等主动动作释放。

当前最重要的产品原则：

- 查看优先，编辑克制。
- 密钥默认可用，不可见。
- 字段是对象，不是文本。
- 用户教过一次规则，App 全局都应该懂。
- 日常解锁优先走系统授权，文档密码只是主动选择的高级模式。

## 目标用户

第一目标用户是个人开发者，尤其是本机有一份敏感 txt 的人：

- 记录服务器、OnePanel、数据库、API Key、SSH、恢复码等信息。
- 平时主要是查看和复制，很少新增修改。
- 讨厌 txt 没分类、长字符串不好复制、密码管理器太老太重。
- 需要本地、轻量、可控、现代化的体验。

## 已实现 MVP

工程位置：`/Users/mac/Site/thinking`

可运行 App：

- `PrivDoc.app`
- `./run.sh`
- `./build_app.sh`

已实现功能：

- macOS SwiftUI 原生桌面 App。
- 新建/打开本地 `.privdoc`。
- 查看、编辑、历史、规则、设置五个模式。
- 查看态自动拆分 `字段名：字段值`。
- 支持 `：`、`:`、`=` 三种字段分隔符。
- 密钥字段默认显示为“已隐藏，双击复制”。
- 普通字段和密钥字段都可双击复制。
- 剪贴板清理可配置，支持自动清理和手动清除。
- 自动清理只清除 PrivDoc 自己写入的那次剪贴板内容。
- 支持 `{{...}}` 无理由隐藏任意文本片段。
- 正文里的隐藏片段会以行内隐藏 chip 渲染，双击复制原文。
- 搜索标题、字段名、非密钥字段值。
- 编辑态保留 Markdown-ish 原文。
- 编辑保存会生成历史版本。
- 历史页支持字段级 diff，密钥 diff 默认脱敏。
- 规则页支持全局识别规则。
- 规则包括自定义密钥词和强制普通字段。
- 查看态右键字段可把字段名加入密钥规则或普通字段规则。
- 保存 `.privdoc` 支持两种解锁模式：
  - 系统授权：默认模式，使用 macOS Keychain + Touch ID / Mac 登录密码。
  - 文档密码：主动选择，适合跨设备迁移。
- 兼容旧版文档密码 `.privdoc`。

## 2026-07-01 精细化修复

本轮针对真实体验截图做了一次全局 polish。

已修复：

- Toast 复制提示从黑底深字改为浅色高对比浮层，避免“背景和文字糊在一起”。
- Toast 位置改到右下角，宽度受控，不再贴在窗口底部中央。
- Toast 增加状态图标，复制/保存/锁定/错误区分更清楚。
- 历史详情页 header 固定，版本标题、时间和“恢复这个版本”按钮不会被内容滚走。
- 历史详情页只有 diff 和原文预览区域滚动。
- 历史版本左侧长标题改为中间截断，避免撑爆列表。
- 历史 diff 的状态和密钥值文案改为中文。
- 历史原文预览默认脱敏密钥，避免在历史页重新暴露旧 secret。
- 保存无变更时提示“没有内容变更”，不再误导用户以为生成了新版本。
- 恢复版本、规则增删、解锁/保存/锁定等 Toast 文案统一中文。
- App 菜单命令改为中文。

## 2026-07-01 剪贴板策略

新增剪贴板清理配置。

已实现：

- 新增“设置”页。
- 剪贴板清理支持 5 秒、15 秒、30 秒、1 分钟、5 分钟、手动清除。
- 默认仍为 15 秒，保持安全默认。
- 选择“手动清除”时会提示风险。
- 顶部栏在剪贴板含 PrivDoc 内容时显示“清除剪贴板”按钮。
- App 锁定时会尝试清除 PrivDoc 写入的剪贴板内容。
- 清理时会检查 pasteboard `changeCount`，只清理 PrivDoc 自己写入的那次内容，避免误删用户之后复制的新内容。
- 旧版 `clearClipboardAfterSeconds` 设置会自动迁移到新的 `ClipboardClearDelay`。

## 2026-07-01 显式隐藏标记

新增轻量显式隐藏语法。

已实现：

- `{{abc123}}`：无理由隐藏，查看态隐藏，复制时复制 `abc123`。
- `{{abc123}}` 单独成行时，会渲染成一个隐藏段落。
- `备注：公开 {{私密}} 公开`：正文里只隐藏双大括号包住的片段，其他内容保持原样。
- `字段：{{私密}}`：字段值隐藏，复制时复制原始内容。
- `@secret`：行尾标记，强制该字段为密钥。
- `@text`：行尾标记，强制该字段为普通文本。
- `#secret` / `#text`：兼容同类行尾标记。
- 双大括号隐藏优先于行尾标记、用户规则、内置规则和自动类型判断。
- 规则页增加“显式标记”说明和示例。

产品判断：

- 不采用 `<*>...</*>`。那只是讨论时的例子，不作为 PrivDoc 语法。
- 移除 `{{secret:...}}` 设计。用户不需要解释为什么要隐藏一段文本。
- 存储层仍是整个 `.privdoc` 加密；双大括号控制的是解锁后的查看态渲染和复制行为。

细节修复：

- 正文里的隐藏片段 chip 调小为行内徽标，不再使用字段级大胶囊样式。
- 自定义 FlowLayout 放置时改为行内垂直居中，避免隐藏 chip 超出中文文本基线。

验证：

- `swift build` 通过。
- `./build_app.sh` 重新打包成功。
- Computer Use 截图验证受本机权限/远程连接状态限制，未拿到可靠截图；本轮主要通过源码审查、编译和用户截图对应问题修复验证。

## 当前安全实现

加密文件格式：

- 文件后缀：`.privdoc`
- 外层 envelope：JSON
- 明文 payload：JSON 后整体加密
- 对称加密：AES-GCM

文档密码模式：

- KDF：PBKDF2-HMAC-SHA256
- 迭代次数：310,000
- 每次保存使用随机 salt 和 nonce

系统授权模式：

- 生成随机 256-bit vault key
- vault key 存入 macOS Keychain
- Keychain 访问控制要求 user presence
- 打开时通过 Touch ID 或 Mac 登录密码授权

重要边界：

- 系统授权模式体验更好，但绑定当前 Mac 的 Keychain。
- 系统授权 `.privdoc` 直接拷到另一台电脑无法解锁。
- 文档密码模式可迁移，但需要用户记住密码。
- 当前还没有自动锁定、内存安全清理、Argon2id。

## 关键产品决策

### 1. 不是做 KeePassXC 翻版

方向不是补齐传统密码管理器功能，而是解决“隐私文档使用不爽”：

- txt 没分类。
- 长字符串不好选中复制。
- 修改后怕搞坏。
- 密钥查看时容易暴露。

### 2. 查看与编辑分离

隐私文档大多数时候用于查看和复制，不是编辑。

默认进入查看态，编辑必须明确切换。这样减少误改，也让界面围绕“使用字段”而不是“写文档”设计。

### 3. 密钥默认不显示

密钥不是拿来看的，是拿来用的。

查看态里密钥字段默认只展示用途，不展示内容。双击后静默写入剪贴板，只提示已复制。

### 4. 原文仍是真相

编辑态不是复杂表单，而是 Markdown-ish 原文。系统解析原文生成查看态字段。

这保留了 txt 的自由度，同时让查看态拥有结构化体验。

### 5. 规则是全局配置

最初考虑过把识别规则写入每份 `.privdoc`，后来改成全局配置。

原因：从 UX 看，用户是在训练 App 理解自己的写法，不是在配置某个文件。用户教一次 `授权码` 是密钥，之后所有文档都应该懂。

### 6. 解锁默认用系统授权

最初只有文档密码。后来改成：

- 默认系统授权。
- 主动选择文档密码。

这更符合 macOS 用户习惯，类似 Chrome 查看已存密码时使用系统身份确认。

## 主要源码文件

- `Package.swift`：Swift Package 配置。
- `Sources/PrivDoc/PrivDocApp.swift`：App 入口。
- `Sources/PrivDoc/Models.swift`：数据模型。
- `Sources/PrivDoc/DocumentParser.swift`：文档解析和字段类型识别。
- `Sources/PrivDoc/CryptoBox.swift`：加密 envelope、AES-GCM、PBKDF2、系统授权加密接口。
- `Sources/PrivDoc/KeychainVaultKeyStore.swift`：macOS Keychain vault key 保存/读取。
- `Sources/PrivDoc/VaultStore.swift`：App 状态、打开保存、复制、历史、规则管理。
- `Sources/PrivDoc/ContentView.swift`：SwiftUI 界面。
- `Sources/PrivDoc/DiffEngine.swift`：字段级 diff。
- `README.md`：运行说明。
- `run.sh`：开发运行脚本。
- `build_app.sh`：打包 `PrivDoc.app`。

## 2026-07-01 系统授权保存修复

用户反馈系统授权保存时出现 `-34018`，含义是缺少 Keychain entitlement。

已修复：

- 系统授权模式保存前，先主动拉起 macOS 身份验证。
- 授权通过后再创建随机 256-bit vault key。
- vault key 写入 Keychain 后，再写入 `.privdoc` 文件。
- 保存按钮增加“保存中...”状态，防止重复点击。
- Keychain 错误文案改为中文，并在 `-34018` 时提示改用“文档密码”或使用正式签名版本。

后续修正：

- 曾尝试给 adhoc 签名的 SwiftPM app 加 `keychain-access-groups` entitlement，但 macOS 会直接拒绝启动，表现为“应用程序无法打开”。
- 已撤销 `PrivDoc.entitlements`，`build_app.sh` 恢复为普通 adhoc 签名。
- 当前 `PrivDoc.app` 已验证可以启动。
- 开发期不硬加 Keychain group；正式发布时应使用 Xcode 项目和 Developer ID/Team ID 正式签名后再配置 Keychain entitlement。

## 2026-07-01 查看页目录语义修复

用户反馈查看页左侧 `Section` 意义不明，点击“全部”只看到标题，且第一行一级标题不应该被当成一块 Section。

已修复：

- 左侧标题从 `SECTIONS` 改为“目录”。
- 左侧入口从“全部”改为“全文”，语义更像阅读整篇文档。
- `# 一级标题` 现在只作为文档标题，不进入目录。
- `## 二级标题` 才作为目录项。
- 点击“全文”会显示文档标题和所有目录内容，不再只显示第一块。
- 点击某个目录项只显示该目录下的内容。
- 如果文档没有 `##`，正文内容会归入隐式“正文”块；纯标题和空行不会生成无意义目录项。
- 移除旧的 `parseWithDocumentSection` 解析路径，避免 H1/H2 语义混乱。

产品判断：

- 一级标题回答“这份密档叫什么”。
- 二级标题回答“这份密档有哪些可跳转的内容块”。
- 查看页左侧不是技术概念里的 section 列表，而是用户理解里的目录。

## 已验证

最近一次验证：

```bash
swift build
```

结果：通过。

最近一次打包：

```bash
./build_app.sh
```

结果：生成 `PrivDoc.app` 成功。

UI 验收说明：

- 已尝试通过 Computer Use 打开本地 `PrivDoc.app` 做截图验收。
- 当前 Codex 的 macOS 辅助功能/录屏权限未授予，无法可靠读取窗口截图。
- 本轮已通过源码审查、`swift build` 和 `./build_app.sh` 验证。

## 待做优先级

最值得下一步做的不是大功能，而是把真实自用体验打磨顺：

1. 自动锁定
   - 闲置 N 分钟锁定。
   - Mac 睡眠/锁屏时自动锁定。
   - 解锁状态 UI 更明显。

2. 解锁方式转换
   - 系统授权 -> 文档密码。
   - 文档密码 -> 系统授权。
   - 避免用户一开始选错后没法改。

3. 导入体验
   - 从现有 txt 导入。
   - 首次导入后给解析预览。
   - 提示疑似密钥字段。

4. 密钥字段编辑体验
   - 查看态右键“替换密钥”。
   - 编辑态密钥默认隐藏。
   - 修改 secret 不必先显示旧 secret。

5. 规则增强
   - 支持导入/导出全局规则。
   - 支持字段值模式，比如 `sk-`、`ghp_` 自动识别为密钥。
   - 支持更明确的手动标记语法，例如 `@secret`。

6. 快捷搜索窗口
   - 全局快捷键呼出。
   - 搜索字段。
   - 回车复制默认字段。

7. 安全增强
   - KDF 从 PBKDF2 升级到 Argon2id。
   - 更明确的剪贴板管理提示。
   - 检测剪贴板管理器风险。
   - 更完整的内存清理。

## 当前风险和注意事项

- 这是 MVP，可以开始自用，但真实数据迁入前建议保留原 txt 离线备份。
- 系统授权模式下，必须保留当前 Mac 的 Keychain；迁移文件前要转换成文档密码模式。
- 当前还没有自动锁定，不要长时间解锁后离开电脑。
- 历史版本会保存旧文档内容，删除/修改密钥后历史里仍有旧值，虽然 diff 脱敏且文件整体加密。
- 规则是全局配置，不跟 `.privdoc` 走；换机器后需要重新配置，后续应做导出/导入。

## 下一次继续时的建议入口

先体验真实数据的一小段，不要一次性全量迁入。

推荐下一轮优先做：

1. 自动锁定。
2. 解锁方式转换。
3. 导入 txt + 解析确认。

这三个会直接决定它能不能真正替代原来的敏感 txt。
